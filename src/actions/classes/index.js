import fetch, { FETCHED_CLASSES, FETCHED_STUDENTS} from './fetch'
import loading from '../loading'
import loadError from '../loadError'
import create, {CREATED_CLASS} from './create'

export {
  FETCHED_CLASSES,
  FETCHED_STUDENTS,
  CREATED_CLASS,
  fetch,
  create,
  loading,
  loadError
}
