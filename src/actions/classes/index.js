import fetch, { FETCHED_CLASSES, FETCHED_STUDENTS, FETCHED_ONE_CLASS} from './fetch'
import loading from '../loading'
import create, {CREATED_CLASS} from './create'
import patch, {PATCHED_CLASS} from './patch'

export {
  FETCHED_CLASSES,
  FETCHED_STUDENTS,
  FETCHED_ONE_CLASS,
  CREATED_CLASS,
  PATCHED_CLASS,
  fetch,
  create,
  patch,
  loading
}
